// where global repos, models, datasource are defined

import { IDataSource, IRepository} from "./types";

// repositories
export const Services:{name?:string, Service?:any} = {}


// models
export const Models:{name?:string, ModeL?:any} = {};

// data sources
export const DataSources:{name?:string, DataSource?:IDataSource} = {}
